import { MultiOrderByPipe } from './multi-order-by.pipe';

describe('MultiOrderByPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiOrderByPipe();
    expect(pipe).toBeTruthy();
  });
});
